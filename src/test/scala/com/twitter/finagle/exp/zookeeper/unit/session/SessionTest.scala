package com.twitter.finagle.exp.zookeeper.unit.session

import com.twitter.finagle.exp.zookeeper.session.SessionManager
import org.scalatest.FunSuite

class SessionTest extends FunSuite {
  trait HelperTrait {
    val sessionManager = new SessionManager(true)
  }

  test("should init correctly"){

  }
  test("should not init correctly"){

  }
  test("should reinit the session"){

  }
  test("should not reinit the session"){

  }
  test("should reset the session"){

  }
  test("canConnect should respond correctly"){

  }
  test("canClose should respond correctly"){

  }
  test("canReconnect should respond correctly"){

  }
  test("should close correctly"){

  }
  test("should prepareClose correctly"){

  }


}
